#!groovy

properties ([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    options {
        timestamps()
    }
    stages {
        stage ("SSH to server") {
            steps {
                step {
                    sh 'ssh root@centmachine \'hostname\''
                }
            }
        }
    }
}
